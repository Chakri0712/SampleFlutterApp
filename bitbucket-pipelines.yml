image: cirrusci/flutter:latest

pipelines:
  branches:
    master:
      - step:
          max-time:
          size: 2x
          script:
            - flutter pub get
            - flutter analyze
            - flutter build apk
          trigger: automatic
          artifacts:
            - "build/app/output/apk/release/*"
